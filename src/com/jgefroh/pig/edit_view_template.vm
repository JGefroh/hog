#set ($modelName = $model.name.toString())
#set ($moduleName = ${model.packageName.toString().substring(0, ${model.packageName.lastIndexOf(".")})} + "." + ${modelName} + "Module")
#set ($modelNamePlural = ${FormatUtil.pluralize(${modelName.toLowerCase()})})
<!--
 * Autogenerated by PIG.
 * @author Joseph Gefroh
-->
<div class="container">
    <div class="row">
        <div class="col-xs-12">
            <h3>Edit ${modelName}</h3>
        </div>
    </div>
    <div class="row">
        <div data-ng-if="!operations.isLoading"
             class="col-xs-12">
             
#foreach ($field in $model.fieldDefinitions)
            <div data-text-input-field
                 data-label="${FormatUtil.pascalCase(${field.name})}"
                 data-model="input.${field.name}"
                 data-help-message="This field entry was automatically generated."></div>
#end
            <span class="fa fa-lg fa-spinner fa-spin" data-ng-if="isFormDisabled"></span>
            <button class="btn btn-success"
                    data-ng-click="save(input)"
                    data-ng-disabled="isFormDisabled"><span class="fa fa-save"></span>
                Save
            </button>

            <button class="btn btn-link"
                    data-ng-click="cancel()">Cancel</button>
        </div>
        <div data-loading-indicator data-message="'Loading ${modelName.toLowerCase()}...'" data-is-loading="operations.isLoading"></div>
    </div>


</div>
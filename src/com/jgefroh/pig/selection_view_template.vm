#set ($modelName = $model.name.toString())
#set ($moduleName = ${model.packageName.toString().substring(0, ${model.packageName.lastIndexOf(".")})} + "." + ${modelName} + "Module")
#set ($modelNamePlural = ${FormatUtil.pluralize(${modelName.toLowerCase()})})
<!--
 * Autogenerated by PIG.
 * @author Joseph Gefroh
-->
<div class="container">
    <div class="row">
        <h2 class="text-center">Select a ${modelName}</h2>
        <div data-ng-if="!operations.isLoading"
             class="col-xs-12">
            <button data-ng-click="create()"
                    class="btn btn-success">
                        <span class="fa fa-plus"></span>
                        Create ${modelName}
            </button>
            <table  class="table table-bordered table-striped">
                <tr>
                    <th>Actions</th>
#foreach ($field in $model.fieldDefinitions)
                    <th>${FormatUtil.pascalCase(${field.name})}</th>
#end
                </tr>
                <tr data-ng-repeat="${FormatUtil.camelCase(${modelName})} in ${FormatUtil.pluralize(${FormatUtil.camelCase(${modelName})})}">
                    <td>
                        <button data-ng-click="edit(${FormatUtil.camelCase(${modelName})})"
                                class="btn btn-default btn-xs"><span class="fa fa-share"></span> Open </button>
                        <button data-ng-click="showRemove(${FormatUtil.camelCase(${modelName})})"
                                class="btn btn-default btn-xs"><span class="fa fa-remove"></span> Delete </button>
                    </td>
                    
#foreach ($field in $model.fieldDefinitions)
                    <td> {{ ${FormatUtil.camelCase(${modelName})}.${field.name} }}</td>
#end
                </tr>
            </table>
        </div>
        <div data-loading-indicator data-message="'Loading ${modelNamePlural}...'" data-is-loading="operations.isLoading"></div>
    </div>
</div>
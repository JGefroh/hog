#set ($modelName = $model.name.toString())
#set ($moduleName = ${model.packageName.toString().substring(0, ${model.packageName.lastIndexOf(".")})} + "." + ${modelName} + "Module")
#set ($modelNamePlural = ${FormatUtil.pluralize(${modelName.toLowerCase()})})
/*
 * Autogenerated by PIG.
 * @author Joseph Gefroh
 */
(function() {
    function ${modelName}Routes(${FormatUtil.getDollarSign()}stateProvider, ${FormatUtil.getDollarSign()}urlRouterProvider) {
        ${FormatUtil.getDollarSign()}stateProvider
            .state('${modelName.toLowerCase()}_selection', {
                url: '/${FormatUtil.pluralize(${modelName.toLowerCase()})}/selection',
                templateUrl: 'modules/${modelNamePlural}/TaskSelectionView.html',
                controller: 'TaskSelectionController'
            })
            .state('${modelName.toLowerCase()}_edit', {
                url: '/${FormatUtil.pluralize(${modelName.toLowerCase()})}/edit/:id',
                templateUrl: 'modules/${modelNamePlural}/TaskEditView.html',
                controller: 'TaskEditController'
            });
    }
    angular
        .module('${moduleName}')
        .config(['${FormatUtil.getDollarSign()}stateProvider', '${FormatUtil.getDollarSign()}urlRouterProvider', ${modelName}Routes]);
})();
